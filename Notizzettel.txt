============================

Seminar Modern C++

Peter Loos

30+ Jahre:  SW-Entwicklung / Training

Pascal, C/C++, Java, C#, Python, Mobile Progr. (Android (Java), Flutter (Dart)) 

Back-to-the-Roots: C/C++

Guten Morgen 

===========================

10.30 ...

12.30 Mittagpause ---

15 ...

17 ...

===========================

Online:

== Visual Studio Community: MSVC
== Visual Code:  GCC , MinGW, ...

== Github

===========================

Inhalt:

Agenda

==> Was soll es sein

Was MUSS DRIN sein

Was muss nicht drin sein ...

Was sind meine Erwartungen

===========================

Donnerstag:

std::initializer_list  : Jepp

Utility-Klassen:
   std::optional   : Jepp
   std::span / std::string_view
   std::variant ========>  Type Traits

Ausnahmen
   std::expected : Jepp

constexpr  // vs #ifdef

Structured Binding:

RAII : Jepp

UDL:   User Defined Literal

Variadische Template ==> Folding


Wenn die Zeit reicht: Spaceship Operator


===========================


C++ 11

"C lastig geschrieben"

"Wie macht man es gut"

Im Sinne neuer Techniken ....

Pass-by-Referenz ?!?
Pass-by-Value    ?!?

Werte-Semantik vs. Referenz-Semantik

Interfaces

Templates

Beispiel für modernen C++ Code

STL:  std::vector

== Container: Ja
== Algorithmen: Eher nein

== Vergleich mit Python

== Typsicherheit:

   Memory Overflow

== new / delete: Dynamische Daten

==========================================

auto, STL, Algothmen

STL Iterator 

==========================================

Unsere Agenda:
--------------

"Wie macht man es gut"

a) Kern C++

b) STL - Verständnis

   C++ OHNE STL:  Neeeeeeeeeee

c) auto, Lambda, ...

d) Wert-Semantik, Referenz-Semantik

Die Zusatz-Dateien:   Seminar_01_Thema_...

=======================

STL: 

= Container
= Iteratoren
= Algorithmen
= Aufrufbare Objekte

Parcours:

a) Klassisch // "veraltet" // Vor C++ 11

b) Modern // mit neuen Konzepten

=========================

Namensraum  // namespace

Die ganze STL befindet sich in einem Namensraum: std   // Standard

void main_seminar_stl_introduction()
{
    using namespace std;
    vector<int> numbers;
}

I wouldn't go for this !!!!!!!!!!

======================

Go for std::size_t

    for (int i = 0; i != 100; i++) {

	for (std::size_t i = 0; i != 100; i++) {

Whyyyyyyyyyy

Etwas pedantisch:  i kann negative Werte annehmen.

VORSICHT:

std::size_t n;

std::size_t n = -1;   // Das GEHT NICHT !!!!!!!!!!!!


if (n >= 0)           Shit;     n ist IMMER groesser gleich 0

========================================

Zu std::vector:

https://github.com/pelocpp/cpp_clean_performant_secure/blob/master/Clean_Performant_Code/Data_Structures_and_Algorithms/Readme_STL_Container.md#link4


Ein std::vector ist sehr ähnlich zu einer Python  list.

Länge ist dynamisch:

l.append();   // Python

v.push_back();   // C++

neue Cap. = alte Cap. * 1.5;

Go for reserve, um unnötige Allokationen zu vermeiden.

==========================================

for-Schleife in Python:

range    // Schrittweite

==========================================

Gedanke der STL:

Transparenz / Austauschbarkeit von Containern.


Feature: STL Iterator

STL Iterator  ===> Was ist das ?? Was beschreibt das ?? 

===>  Eine Position in einem STL Container

Wie kommen wir zu einer Position:

STL Container:  begin(), end()

Von welchem Typ ist so eine Position ???

Idee: Jeder Container bringt den Typ für die Position mit :)

Nebenbemerkung: using // typedef

==========================================

Was kann / hat eine Position:

Methoden:  Position ist NICHT der Wert an der Position.

pos.value();
pos.next();

Hätte man so machen können:

Operatoren:  Besser lesbar.  Ja, durchaus ..............

pos.value();     // *pos
pos.next();      // pos ++;


====================================================

Aufrufbare Objekte

Was ist ein aufrufbares Objekt ???

Why ???????????????????????????????????

Um über Objekte Zugriff auf Instanzvariablen zu erhalten:  OO


a) Wir brauchen diesen Operator 

b) Man hätte auch das Feature über eine Methode realisieren können.


Wann brauche ich einen aufrufbaren Operator ??

Zusatzinformationen:

Beispiel:

Suchen:  find_if 

template< class InputIt, class UnaryPred >
InputIt find_if( InputIt first, InputIt last, UnaryPred p );

	unary predicate which returns ​true for the required element.

	bool (int parameter) ;    // Was wird eigentlich gesucht.

	int what muss in das aufrufbare Objekt als Instanzvariable

    std::find_if(
        numbers.begin(),
        numbers.end(),
        myPrinter
    );

Bin jetzt bei aufrufbaren Objekten angekommen:   Lambdas !!!!!!!!!!!!!!!!!!!!!!!!!!

=========================================================

Was können wir besser machen:


A) Syntax

    std::vector<std::size_t>::iterator pos = numbers.begin();
    std::vector<std::size_t>::iterator end = numbers.end();

B) SYNTAX:

    std::for_each(
        numbers.begin(),
        numbers.end(),
        myPrinter    // egal ob freie Funktion oder Callable
    );

==========================================================

Referenz: Hmmmmmmmm ... nicht ganz klar .........

Ist eine Python- und eine C++-Referenz IN ETWA dasselbe ????

NEIN

Why ??????????????????????????????????????

C++

Eine Referenz ist ein Stellvertreter für eine VORHANDE Variable

int n = 123;

int& rn = n;   // Hinter den Kulissen: in rn ist die Adresse von n

Python:

==========================================================

Stack und Heap

void malZwei(int value)
{
    int m = value;
00007FF7791A79BF  mov         eax,dword ptr [value]  
00007FF7791A79C5  mov         dword ptr [m],eax  



void malZweiEx(int& value)
{
    int m = value;
00007FF778FDE400  mov         rax,qword ptr [value]    // Indirektion
00007FF778FDE407  mov         eax,dword ptr [rax]  
00007FF778FDE409  mov         dword ptr [m],eax  




Heap: Wird in C++ durch new / delete erreicht / verwaltet ....

Python: Einfacher: ALLE WERTE sind am Heap

Am Stack: Ausschließlich nur Adressen / Referenzen, die auf die Daten auf dem Heap zeigen.


// =======================================================================================

Werte- versus Referenz-Semantik:
-------------------------------

>>> Referenz: auf den richtigen Pfad bringen: Go for References soweit wie möglich !!!! C++

============================================

auto:

Tritt - SYNTAX - an DREI verschiedenen Stellen auf:

a) Vereinbarung von Variablen

B) Rückgabetyp

C) Parameter

===============================

// old, "classical" syntax
std::string toString(int value);

// new syntax with trailing return type
auto toString(int value) -> std::string;

Whyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

Antwort: Hat nichts mit "normalen" Funktionen zu tun !

Sondern: Hintergrund ist die generische Programmierung.

Man kann den Ergebnistyp "berechnen" mit decltype.

============================================

Wiederholung:

In C++: Implizite Typkonvertierung

int + double ==> double

int + long ==> long

short + int ==> int

etc.

============================================
============================================

Lamdas
------

Wie sieht der C++ Compiler einen Lambda-Ausdruck:

Beobachtung:

"Lambda-Funktion"

FALSCH !!!!!!!!!!!!

Was denn dann ???

Ein Lambda ist ein OBJEKT !!!!!!!!!!!!!!!!!!!

Cpp Insights 

void test()
{
    std::string s{ "Hello" };

    auto func = [=]() { std::print("{}", s); };

    func();
}


Okay, ein Lambda ist ein Objekt:

Was haben Objekte ???

Methoden und Eigenschaften (Instanzvariablen)

=====================================================

Initialisierung

std::initializer_list

Move-Semantik

Smart - Pointer

==================================

Soll ich Legacy Code umstellen: NEIN.

==> Fehler möglich !!!

Neuer Code: Up to You: 

==================================

POD: Plain Old Data Type


CVTSI2SD — Convert Doubleword Integer to Scalar Double Precision Floating-Point Value


Zero-Cost Abstraction

===================================

Verschiebesemantik (Move Semantics):

Hilfsmittel:

Es gibt ab C++ 11 einen neuen, zweiten Referenztyp: RValue

SYNTAX: std::string&&

Whyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy:  Verschiebesemantik

Der "alte" heißt LValue.

SYNTAX: std::string&

Referenz:

Vorab-Thema:

int n = 123;

int& rn = n;     

Point p = { 1, 2 };

Point& rp = p;

"Objekt mit Namen"

Temporäres Objekt: Kein Name vorhanden


// ====================================================

Verschiebesemantik:

..: ... fd0





BigData::BigData(const BigData& data)

Kopier-Konstruktor   WICHTIG !!!!!!!!!!

Demo:


a) ....b70     Zweimal

b) ... 4b0     Wurde überschrieben


Gültig: Stammt von einem new (Jepp)  UND WURDE NOCH NICHT mit DELETE freigegeben.

  Ein mit delete freigegebene Adresse ist NICHT MEHR Gültig.

  "double delete"


  Rule-of-Three:  Maschinenraum von C++

  ===> Wenn in Objekten, die wir verfasst haben, new in den Instanzvariablen auftritt.

Rule-of-Three habe ich noch nie gesehen:

new: Alternative:   std::vector, ...  (Rule-of-Three im Bauch)

Globale Variablen: kein new

Implizit:  Python:  copy.deepcopy   ===> Analogie:  Rule-of-Three von C++

=========================================================

Verschiebe-Semantik:

1.) .... ef0:   Tmp. Objekt (am Stack)  // wurde freigegeben

2.) .... 120:   Dieses Objekt ist im Vektor


Beobachtung:

2 Objekte, um eines davon in den Container zu bekommen !

>> Wert- UND Referenz-Semantik

Ab  C++ 11: Verschiebe-Semantik:

1.) ... ef0

============================================

noexcept

Exception:  throw / catch

Ist eine perfekte Lösung:

Emebedded SW // hoher Performanz- Anforderungen:

Das Laufzeit

Optimierungsmöglichkeit:

throw, aber IM SELBEN Unterprogramm KEIN catch:

Dann wird die Ausnahme zum Rufer propagiert / durchgeschleust / hoch gereicht:

Stack Unwinding:  Mit noexcept abschalten.

Wann kann man sich seiner Sache sicher sein?

zB: Nur Wertzuweisungen.

============================================

Rule of Zero:

== Es dürfen keine Zeiger vorhanden sein, die mit new allokiert wurden.

== Verwenden Sie Container Klassen der STL.

== Es sind auch dynamische Daten  erlaubt (new) 
   Verwenden Sie Smart Pointer Klassen.

   Diese kümmern sich um den Speicher selbst.  // delete


VORSICHT:

Deklarieren oder definieren Sie dann keine Operationen der Rule of Three
oder Rule of Five für Ihre Klasse.

Hmmm, ist das wichtig ???


==============================================================

int* ip = new int[10];

Problem:


// delete[] ip;  // irgendwann :))


Detected memory leaks!
Dumping objects ->
C:\Development\Seminar_Modern_Cpp_August_206\GeneralSnippets\MemoryLeaks\MemoryLeaksDetection.cpp(24) : {153} normal block at 0x000001806FA95BE0, 40 bytes long.
 Data: <                > CD CD CD CD CD CD CD CD CD CD CD CD CD CD CD CD 
Object dump complete.
The program '[26236] Cpp_Modern.exe' has exited with code 0 (0x0).

Das ist das Problem mit new / delete !

Lösung:  Diszipliniertes Arbeiten.

// ================================================================

Alternative:

Kein new / delete verwenden (Ausnahmen).

Idee:

A) C++:  OO  

B) OO:  Klassen:  Konstruktoren / Destruktoren

C) Wann werden Konstruktoren aufgerufen:

   Am Ende der Methode / Scopes  {   }

   // In Java gibt es das nicht ... in Python gibt es das nicht.

D) Man verstecke den Zeiger (new) in einer Wrapper-Klasse (Hüllenklasse)

   Konstruktoren:  da muss der Zeiger irgendwie an die Hülle übergeben werden.

   Destruktor:  delete !!!!!!!!!!!


E) Eine erste Umsetzung:  auto_ptr   ==> deprecated.

F) Eine zweite Umsetzung:   2 Strategien

   Mehrere Klassen:

   std::unique_ptr
   std::shared_ptr und std::weak_ptr

i) Strategie std::unique_ptr:

   Es gibt im laufenden Programm zu einem std::unique_ptr Objekt
   genau EINEN Besitzer.

   zB: Kann NICHT kopiert werden !

   Man kann durchaus mit einem std::unique_ptr Objekt arbeiten.

i) Strategie std::shared_ptr:

   Es gibt im laufenden Programm von einem std::shared_ptr Objekt
   MEHRERE Besitzer.

   zB: Kann kopiert werden !

   Hmmm, wer ruft delete auf ??????????????????

   Nicht - trivial:  Für die Anzahl der Besitzer:  Referenz-Zähler

   Erst wenn dieser auf 0 geht, kann delete aufgerufen werden !

   3 => 2 => 1  => 0

   ==> Ähnlichkeiten: Prinzipien eines GCs.


===============================================================


Kann man das Zurück Kopieren von Objekten aus einer Funktion optimieren ???

Return Value Optimization zum Beispiel

"Stack Frame":

Micro Computer Programming:

Unterprogrammaufruf - Mechanismus:

===============================================================

Go for Smart Pointer
--------------------

std::unique_ptr

std::shared_ptr

Hmmmmm, eigentlich ist der std::unique_ptr die erste Wahl:

Er legt fest, wer der Besitzer ist !!!

===============================================================


std::unique_ptr

std::shared_ptr

Wir haben ein Problem: std::shared_ptr funktioniert nicht immer ?!?!?!?!?!?!?!?!?!?!


Problem: Verstehen  // Wie löst er das Problem ???

Problem: Lösung   std::weak_ptr  Wie funktioniert dieser ???

Vorsicht: Der Name lock könnte irritierend / missverständlich sein !?!?!?!?!?

Besser:  access  Noch besser:   std::shared_ptr  tryAccess ()


Frage:

Observer - Pattern

Was ist das: Beobachten

a) Zielobjekt:  Hat Zustand (Wetterdaten / Aktienkurse) / kann sich ändern

b) Andere Objekte: Diese interessieren sich für Änderungen am Zielobjekt

c) Um Änderungen mitgeteilt zu bekommen:
      Andere Objekte melden sich am Zielobjekt an,
      dieses hebt die angemeldeten Objekte in einer Liste auf (Pointer, ... , Smart Pointer).



Resumee
=======

Aus bestimmten Gründen ist der std::unique_ptr ausgeschieden.

std::shared_ptr:

Wie arbeite ich mit der Klasse std::shared_ptr.

Was habe ich zu beachten?

A) Sollte ich in einem Programm ausschließlich  std::shared_ptr Variablen verwenden ???

   Nieeeeeeeeeeeeeeee

b) Beispiel:

   Daten:    std::shared_ptr
 
   Visualisierung:  Worauf sollte eine Visualisierung arbeiten?

   std::weak_ptr:   lock   => empty:   Diese Aktie wird aktuell nicht gehandet / es sind keine Daten verfügbar. 

c) 

Observer - Pattern

Was ist das: Beobachten

a) Zielobjekt:  Hat Zustand (Wetterdaten / Aktienkurse) / kann sich ändern  // std::shared_ptr

b) Andere Objekte: Diese interessieren sich für Änderungen am Zielobjekt   // std::shared_ptr

c) Um Änderungen mitgeteilt zu bekommen:
      Andere Objekte melden sich am Zielobjekt an,   attach   //  detach
      dieses hebt die angemeldeten Objekte in einer Liste auf (Pointer, ... , Smart Pointer).


	  std::vector  <  std::weak_ptr <IClient>  >

	  Zustandswechsel:  for (  auto&  client : observer)  ==> lock:  empty  ...hmm, den Beobachter gibt es nicht mehr.


	Neuer SW: Smart Pointer  ( 14 Tage ... dann hast du die drauf )

	std::shared_ptr: ABER so viel wie möglich std::weak_ptr verwenden .... 

std::shared_ptr:   75%   jaaaaaaaaaaa

std::weak_ptr:     99%   nie gehört

=========================================================

static void lambda_01_modern()
{
    std::vector<int> vec{ 1, 2, 3 };

    std::for_each(
        vec.begin(),
        vec.end(),
        [] (auto n) { 
            std::print("{} ", n);
        }
    );

    std::println();
}


Generische Funktionen / Lambdas
==============================

Erlleichtern die Wartbarkeit / Änderbarkeit des Quellcode
bei kleinen Änderungen.

auto als Parameter // der Compiler macht den Rest.

Wiederholung: auto

a) Vereinbarung von Variablen 

b) Rückgabetyp (Lambdas)

c) Als Parameter

====================================

Blick auf Python:

def say_hello(count, message):   <===== Referenzen ( alle 8 Bytes )


    // Stack Frame: Als Wert
    // count:         8 Bytes
    // message:       8 Bytes


	Die Werte hinter den Referenzen sind am HEAP

======================================

Range-based for Loop

12 ways to iterate through a vector 

Hmmm, das hat viel mit der Historie von C++ zu tun.

==============================================

Müssen STL Algorithmen wirklich sein ???

Einsteiger .................

==============================================

C++:  Debug-Modus // Release-Modus

Debug-Modus
Viele Debug Informationen
Optimierung: Eher schlecht

 Release-Modus
 Kleineres Footprint vom Executable
 Debuggen geht nicht
 Optimierung: Sehr stark.

 Warum gibt es zwischen std::array und std::vector
 so große Unterschiede ??????????????????

 MMU:  Memory Management Unit

 A) Deine Arrays liegen im globalen Segment // ist / könnte geladen sein

 B) Deine Vektor-Daten liegen am Heap // MMU: Virtueller Speicher

 Beobachtung:  Hmmm, dynamische Variablen haben einen gewissen Preis.

 ========================================

 Brace Init.

 VORSICHT:    Konstruktoren:  ()  => {}

 Brace Init geht nicht immer !!!!!!!!!!!


 ACHTUNG:

 std::vector<int> numbers { 10 };   // Size: 1 - Wert: 10

 vs

 std::vector<int> numbers ( 10 );   // Size: 10 - 10 Werte mit dem Wert 0

=======================================================

std::optional:

Monadic Interface


int wert;

...

if (wert == 0) ....
  hhhhm 0 könnte ein zulässiger Wert sein

if (wert == -1) ....
  hhhhm -1 könnte ein zulässiger Wert sein
   
VORSICHT: std::size_t


if (wert == MAX_INT) ....
  hhhhm -1 könnte ein zulässiger Wert sein

int wert;
bool isValid;   //  nullopt

Zeichenkette: ""

=======================================================

Resources:

Betriebssystem:

Socket,  SqlConnection, FILE, 

a) open(), attach(), connect()

b) close(), detach(), disconnect():  

SqlConnection:  close()

A) Wie: Diszipliniertes Programmieren

B) RAII - Idiom


https://github.com/pelocpp/cpp_design_patterns/blob/master/Patterns/RAII_Idiom/Resources/Readme.md

Idee:

Dieselbe Idee wie Smart Pointer:  Wir stecken die Resource in ein Hüllenobjekt / Wrapper

Konstruktor:  open(), attach(), connect()


Destruktor ===> close(), detach(), disconnect(), ..


=================================================================




